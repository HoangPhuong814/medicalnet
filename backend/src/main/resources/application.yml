server:
  port: ${PORT:8080}

spring:
  main:
    allow-circular-references: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/medical_net}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1108}
    driver-class-name: org.postgresql.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${SHOW_SQL:false}
    properties:
      hibernate:
        format-sql: false
  sql:
    init:
      mode: ${SQL_INIT_MODE:always}
      data-locations: classpath:sample_data.sql
      continue-on-error: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:phuongtranhoang98@gmail.com}
    password: ${MAIL_PASSWORD:fcve imhs kjeb cwzi}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

jwt:
  signer-key: ${JWT_SIGNER_KEY:9b2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f}
